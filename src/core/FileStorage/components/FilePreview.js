import React from "react";
import FileCopyIcon from "@material-ui/icons/FileCopy";

const FilePreview = () => {
  return <FileCopyIcon color="action" style={{ width: 100, height: 110 }} />;
};

export default FilePreview;
